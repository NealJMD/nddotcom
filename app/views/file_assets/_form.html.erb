<%= form_for(@file_asset) do |f| %>
  <% if @file_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@file_asset.errors.count, "error") %> prohibited this file_asset from being saved:</h2>

      <ul>
      <% @file_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.file_field :content %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.collection_select :post_id, @posts, :id, :title, prompt: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
